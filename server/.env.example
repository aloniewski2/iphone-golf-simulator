# Postgres connection string. Leave unset to keep everything in memory (local testing only).
DATABASE_URL=postgres://golf:secret@localhost:5432/golf
# Port to listen on; most hosts set this for you.
PORT=8080
# 1 when running behind a load balancer that sets X-Forwarded-For (Cloud Run, Fly, App Platform).
TRUST_PROXY=1
